# Dependencies
node_modules/
.pnp
.pnp.js

# Environment
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build outputs
dist/
build/
*.tsbuildinfo

# Testing
coverage/

# Database
*.db
*.sqlite

# Docker
.docker/

# Local scratch files (may contain credentials, never commit)
scratchpad/

# XAVFSIZLIK-AUDIT.md Y-7: candidate resumes, employee photos and EJM
# documents — real personal data — were being committed straight into git.
# Runtime-uploaded content belongs in the volume-mounted uploads/ dir
# (see docker-compose.yml), never in the repo.
apps/backend/uploads/

# XAVFSIZLIK-AUDIT.md P-2: stray backup of an old, unpatched service file
# left tracked in git.
**/*.service.js.bak

# Deploy runbook zaxiralari — DB dump va yangi generatsiya qilingan sirlarni
# saqlaydi (deploy-production.sh yaratadi). Hech qachon commit qilinmasin.
.deploy-backups/
