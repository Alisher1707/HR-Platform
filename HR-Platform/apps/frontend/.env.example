VITE_API_URL=http://localhost:5000/api/v1

# Set to "true" only for the demo/sales-presentation build — shows a small
# "DEMO REJIMI" badge on every page. Leave unset (or "false") in production.
VITE_DEMO_MODE=false
